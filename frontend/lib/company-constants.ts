/**
 * Constants shared between server and client code. Keep this file free of
 * any `next/headers` or other server-only imports so client components can
 * safely import from it.
 */

export const ACTIVE_COMPANY_COOKIE = "prospex_company_id";
